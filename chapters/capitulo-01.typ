= Vetores, Velocidade e aceleração


#set heading(numbering: none)

== Problemas Propostos

#show heading: set align(left)
#show heading: set text(size: 12pt)

=== ÁLGEBRA VETORIAL

#set enum(start: 50)
+ Dados dois vetores quaisquer $bold("A")$ e $bold("B")$, ilustrar geometricamente a igualdade $4 bold("A") + 3(bold("B") - bold("A")) = bold("A") + 3 bold("B")$.

  #set par(first-line-indent: 0pt)
  *Solução:*

  

